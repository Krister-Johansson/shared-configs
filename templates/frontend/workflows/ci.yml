# Thin caller for the shared CI workflow — frontend profile.
# The actual jobs live in Krister-Johansson/shared-configs; dependabot keeps
# the pinned version fresh (pin set per AGENTS.md step 3).
name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: Krister-Johansson/shared-configs/.github/workflows/ci.yml@{{SHARED_CONFIGS_SHA}} # {{SHARED_CONFIGS_VERSION}}
    with:
      run-lint: false # set true when the repo has a `lint` script
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # End-to-end tests — uncomment when the repo has them (script `test:e2e`).
  # Make this a required status check (AGENTS.md step 6).
  # e2e:
  #   name: e2e (playwright)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
  #       with:
  #         persist-credentials: false
  #     - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - run: npm ci
  #     - run: npx playwright install --with-deps
  #     - run: npm run test:e2e
  #     - name: Upload playwright report
  #       if: failure()
  #       uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
  #       with:
  #         name: playwright-report
  #         path: playwright-report/
  #         retention-days: 7
